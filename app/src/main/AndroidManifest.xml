<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.litedoid.orachat"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".OraChatApplication_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".splash.SplashActivity_"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <activity android:name=".controller.auth.NewLoginActivity_"/>

        <activity android:name=".auth.login.LoginActivity_"
                  android:screenOrientation="portrait"/>

        <activity android:name=".auth.register.RegisterActivity_"
                  android:screenOrientation="portrait"/>

        <activity android:name=".chat.list.ChatListActivity_"
                  android:screenOrientation="portrait"/>

        <activity android:name=".chat.detail.ChatDetailsActivity_"
                  android:screenOrientation="portrait"/>

        <activity android:name=".chat.editprofile.EditProfileActivity_"
                  android:screenOrientation="portrait"/>

    </application>

</manifest>